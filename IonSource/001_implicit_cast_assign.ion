// 001 implicit_cast_assign.ion
// Assigning a narrower integer type to a wider one

fn main() -> void {
    var a: s8 = cast(s8)42;
    var b: s64 = a; // implicit widen
    var c: s32 = b; // should fail: narrowing
    println(b);
}