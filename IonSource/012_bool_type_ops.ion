fn main() -> void {
    var t: bool = true;
    var f: bool = false;
    var r := t && f;
    var s := t || f;
    // var x: s32 = t; // should fail: bool is not s32
    var y: s32 = cast(s32)t; // explicit cast: 1 or 0?
    println(r);
    println(s);
    println(y);
}