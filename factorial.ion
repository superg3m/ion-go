fn factorial(x: int) -> int {
    if (x == 0) {
        return 1;
    }

    return x * factorial(x - 1);
}

fn main() -> void {
    defer println(factorial(10));
    var x: int = factorial(5);
    println(x);
}