fn factorial(x: s32) -> s32 {
    if (x == 0) {
        return 1;
    }

    return x * factorial(x - 1);
}

fn main() -> void {
    defer println(factorial(10));
    var x := factorial(5);
    println(x);
}